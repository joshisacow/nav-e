on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
    
jobs:
  test-flask:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with: 
          python-version: '3.11'
      - name: Install requirements
        run: pip install -r requirements.txt
      - run: python main.py
      - name: echo workspace
        run: echo $GITHUB_WORKSPACE
        shell: bash
        
        # insert health endpoint test here
